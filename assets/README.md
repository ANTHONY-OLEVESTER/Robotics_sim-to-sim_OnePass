# Assets

This folder holds small static assets that help explain the public release.

- `pipeline.pdf`: architecture figure copied from the paper assets for quick reference.

The website publishes its own copied public media on the `gh-pages` branch under `assets/`.
The PyPI companion package does not ship these heavier visual assets.
